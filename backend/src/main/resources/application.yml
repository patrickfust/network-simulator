spring:
  application:
    name: Network Simulator
  profiles:
    active:
      - local

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui/index.html

server:
  port: 9898
